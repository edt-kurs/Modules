
&НаКлиенте
Процедура Сформировать(Команда)
	СформироватьНаСервере();
КонецПроцедуры

&НаСервере
Процедура СформироватьНаСервере()
	
	СсылкаОбъект = СсылкаНаСправочник.ПолучитьОбъект();
	
	МетаОбъект = СсылкаОбъект.Метаданные();

	Сообщить("Тест"!);

КонецПроцедуры
